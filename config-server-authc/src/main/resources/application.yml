server:
  port: 5057
spring:
  application:
    name: config-server-authc
  cloud:
    config:
      server:
        git:
          uri: https://github.com/leopard5/yz-springcloud-config.git
          search-paths: config-repo/
          username: leopard5
          password: xixiqi88JK99
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1112/eureka/,http://localhost:1113/eureka/

security:
  basic:
    enabled: true
  user:
    name: user
    password: password123